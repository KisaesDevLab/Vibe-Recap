# QUESTIONS.md — open decisions

Each entry: the question, the recommended answer, and what was done. Decisions here were taken
to keep building; they are not locked and can be reversed. Locked decisions live in docs/PLAN.md §2.

| # | Question | Recommendation (taken) | Phase |
|---|---|---|---|
| Q11 | The staging pass (first-page names/year/software) must run in Python (pdfplumber) but the upload endpoint is Node. | Run it through a second BullMQ queue `recap-stage` serviced by the same worker container at concurrency 4, with the API awaiting the result (20 s timeout). Keeps one PDF parser and keeps the worker offline. Payload is `{stageId, fileId}`, ids only. | 2 |
| Q12 | Client display-name format for auto-created clients. | `Last, First` and `Last, First & Spouse` for joint returns; fuzzy matching also tries `Last, First` alone so a formerly single filer still matches. | 2 |
| Q13 | Where do uploaded bytes live before the preparer clicks Queue? | Encrypted immediately under `blobs/staging/<stageId>/` with the same per-file age keys; metadata in Redis with a 1 h TTL; a 10-minute cron removes staging directories whose Redis row expired. | 2 |
| Q14 | Original filenames may contain client names. | Kept only in the Redis staging row (1 h) for the preparer to recognise files; never written to Postgres or logs. | 2 |
| Q15 | Software signatures in `form-profiles/signatures.yaml` are best guesses ("UltraTax CS", "Lacerte", "CCH Axcess", "GoSystem", "Drake Software", "ProSeries"). | Confirm against real footers from each package; the file is data, no code change needed. | 2 |
| Q16 | What happens after "Re-extract" (identify through recon) succeeds on a job that already had a script and video? | The pipeline continues through script, validate, verify and render, because the old script's extraction hash no longer matches and Approve would refuse it anyway. Re-extract therefore means "re-run everything from identify". | 3 |
| Q17 | Filing-status checkbox detection relies on an "X"/"[X]" token near the status phrase, or on the status being the only one printed. Real layouts draw the box as a glyph or a vector. | Calibrate against real returns from each package; the profile's `filing_status` phrases are data, the marker heuristic is in `mapper.detect_filing_status`. If a package prints nothing detectable, the job fails at extract with "filing status not detected" rather than guessing. | 3 |
| Q18 | Line 4b (IRA) and 5b (pensions) are one schema field `ira_pensions`. | Summed via `combine: sum` in the profile so the schema in PLAN.md §5 stays unchanged. | 3 |
| Q19 | The OCR model is asked for line-level JSON with pixel boxes. GLM-OCR's real output format may differ. | `ocr.parse_ocr_json` is the only place that reads it; adjust the prompt or parser after a real run on the M6. Tests use a stand-in OCR so the overlay and re-extraction path is covered regardless. | 4 |
| Q20 | The verifier must show the matched text highlighted in the PDF viewer. Browser PDF viewers differ: Chrome honours `#page=N`, Firefox (pdf.js) also honours `#search=`. | Links open `/api/jobs/:id/source.pdf#page=N&search=<amount>`; the page is always right, the highlight depends on the browser. A bundled pdf.js viewer would make it uniform (v1.1). | 5 |
| Q21 | Which computed percentages may a script state? | Effective rate (total tax / taxable income), year-over-year AGI and total-tax changes, and withholding as a share of total tax. The validator whitelists them from the observations; the verifier recomputes each from the return's own lines and also accepts tax / AGI. Anything else is flagged. | 5 |
| Q22 | Manual script edits are validated twice: the API applies the TypeScript mirror on save and the worker applies the Python validator before verify. | Keep both; the API check gives instant feedback and the worker check is the gate. If the two ever disagree the worker wins and the job fails at `validate` with the reason shown on the job page. | 5 |
| Q23 | PHASES.md says "concat demuxer with durations, 0.4 s crossfade"; the concat demuxer cannot crossfade. | Each slide is a `-loop 1` input and ffmpeg's `xfade` filter chains them; every slide is shown for its narration length plus the 0.4 s fade so transitions start exactly when a slide's audio ends and sync is exact. The concat demuxer is used for the audio only. | 6 |
| Q24 | Kokoro model files (325 MB + 28 MB) must be inside the worker image because the worker has no internet. | Baked in at build time from the kokoro-onnx release; the `worker_models` volume was removed so the baked files are never hidden by an empty volume. | 6 |
| Q25 | Passkeys (WebAuthn) and TOTP are optional Phase 6 deliverables. | Deferred to v1.1 per Q8; the settings/users page shows their status column as "not enabled". Session cookies plus Argon2id remain the v1 login. | 6 |
| Q26 | Which roles may preview a video before release? | preparer/admin at any stage after render; staff once approved; viewer once released. Every preview is an audited `file.read`. | 6 |
| Q27 | Download file names come from the client name, which is PII in the firm's own filesystem. | Kept: `<Last-First>-<year>-recap.zip` is what a preparer needs to attach to the right client's delivery; the name never leaves the firm. Punctuation is stripped. | 7 |
| Q28 | May staff download approved-but-unreleased deliverables? | No. PLAN §6 says staff download after release; preparers and admins may download at any stage after render to review. Viewers only after release. | 7 |
| Q29 | When does the source-PDF retention window start? PLAN §7 says "after upload" but the pipeline needs the PDF until it is ready (and again for re-extract). | It starts when the job reaches `needs_review` or `failed` (the `ready_at`/`failed_at` timestamp), so "0 days" means "delete as soon as the return is no longer needed", which is what PHASES 8 asks for. Re-extract after the source is purged fails cleanly with "source PDF is missing (purged?)". | 8 |
| Q30 | Extraction, script, and video windows are anchored on release. Jobs that are never released keep those artifacts until they fail or an admin purges the client. | Accepted for v1; the source PDF still purges on its own window, which is the sensitive file. A "needs_review older than N days" window could be added in v1.1. | 8 |
| Q31 | The licensing server API is not documented in the plan. | Assumed contract: `POST https://licensing.kisaes.com/v1/validate` with `{product:"vibe-recap", key, instance_id, seats_in_use, version}` returning `{valid, expires_at?, max_seats?, message?}`. Daily check, 14-day offline grace, read-only (HTTP 402 on writes) afterwards. `services/license.ts` is the only place to change if the real contract differs; the client is injectable and the tests use a fake. | 9 |
| Q32 | Where do form profiles live at runtime so a firm can fix one without rebuilding? | Seeded from the image into `data/form-profiles` on the worker's first start (never overwritten), used by the worker from there, and included in the settings export/import. | 9 |
| Q33 | Invite links need somewhere to live without a new table. | Redis key `invite:<token>` with a 24 h TTL, single use. | 9 |
| Q34 | On this Windows dev box (Docker Desktop VM) one Qwen3-8B script attempt exceeds 180 s. | Timeout is now a firm setting (default 600 s). The M6 is expected to be faster natively; measure there and lower it if warranted. | 9 |
| Q35 | A fresh install with no key (or no reachable licensing server yet) was read-only from the first minute, which blocks evaluation and the first-run experience. | 14-day trial from the day the first user is created; after that, unlicensed means read-only. `LICENSE_ENFORCE=false` disables the gate for evaluation boxes. | 9 |
| Q36 | PLAN §5a says the tax year "must appear in the script exactly once". The real model naturally says "your 2025 return" and "2025 estimated payments" in one script, and the rule adds no safety over the year being correct. | Relaxed to: the return's tax year must appear at least once and no other year may appear. The prompt still asks for it once. | 9 |

## Q&A session 2026-09-05 (Kurt)

| # | Question | Kurt's answer | Effect |
|---|---|---|---|
| Q31/Q35 | Licensing before a key validates | No enforcement yet | `LICENSE_ENFORCE` defaults to false; the check, status page, and trial logic stay in place for when the server exists. |
| Q29 | Source-PDF retention anchor | When the job is ready or failed | Confirmed as built. |
| Q36 | Tax year rule | At least once, no other year | Confirmed as built. |
| Q16 | Re-extract | Continue through render | Confirmed as built. |
| Q25 | Passkeys / TOTP | Defer to v1.1 | Confirmed. |
| Q27 | Download file names | Client name | Confirmed as built. |
| Q15/Q17 | Real returns for calibration | Yes, UltraTax first | Kurt supplies a real UltraTax return, kept local, never committed; profiles calibrated then. |
| Q37 | Script generation model | **Vibe AI Router by default**, DigitalOcean serverless open-source models | Amends L4 and L12: the worker's second and default egress is the router on `vibe_net`; bundled Ollama remains the local option (`llm_provider = ollama`). Task class `recap_script` is registered by the API at startup and starts `local_only` until the firm admin widens it in the router console. Only the prompt (extracted figures, first names, filing status, states, preparer note) leaves the box; the PDF never does. Per the 1040 addendum notes, DigitalOcean-hosted open models are no-retention; passthrough OpenAI models via DO should not serve this class. |
| Q38 | Vibe Appliance packaging (Kurt, 2026-09-05: "ensure the app is compatible with the Vibe Appliance"). | `.appliance/` holds the manifest (validates against `console/manifest.schema.json`), the compose overlay, the env template, and a logo, following `docs/PER_APP_INTEGRATION.md`: env-only config, `ALLOWED_ORIGIN` list, multi-arch GHCR images with latest/vN.M.K/sha tags, `/api/v1/health`, `MIGRATIONS_AUTO` with an explicit `migrate` command, `.appliance/manifest.json`. On the appliance the worker sits on a private internal network behind `vibe-recap-relay` (socat: postgres, redis, vibe-ai-router, vibellm, aliased under their real names) because `vibe_net` is a plain bridge with internet. Every overlay service is pinned to uid 1000 (`dataOwner`), which required the web tier to move to unprivileged nginx on 8080. Two things must land in the Vibe-Appliance repo itself: copies of the overlay and env template under `apps/` and `env-templates/per-app/`, and emergency port 5183 in `docker-compose.yml`'s emergency-proxy publish list. | 9 |
| Q39 | OCR on the appliance. Recap's OCR client speaks Ollama's native `/api/chat` with images; the appliance's local model server (`vibellm`, port 11434) is documented with a `/v1` (OpenAI) URL and may not expose the native API. | Left as is: `OLLAMA_URL=http://vibellm:11434` through the relay; if the native endpoint is absent, scanned packages fail cleanly at `ocr` with a clear message and text-layer PDFs are unaffected. Routing OCR through the router's vision task classes is the v1.1 fix. | 9 |
| Q40 | Kurt (2026-09-05): "after the video is created do we have the ability to request changes via a chat?" | Built as a per-job revision thread: a preparer's plain-English instruction is sent with the current script as the model's last turn; the result passes the same validator, the verifier inside the retry loop, then validate, verify, narrate, slides, mux. A rejected revision (three failed attempts) leaves the previous script, verification, video, and status untouched and shows the reason in the thread. One pending revision per job; every request is audited. Not an open chat: instructions only. | post-plan |
| Q41 | Live run: qwen3 wrote a "you do not owe anything" style sentence on an all-refund return and the verifier's direction check read it as a balance-due claim, failing the job at `script`. | Negated phrasings ("no balance due", "don't owe", "no refund") no longer count as a direction claim; an actual claim is still flagged and the flag now quotes the sentence so the preparer sees what tripped it. Test added. | post-plan |
| Q42 | Live run: qwen3 wrote "total tax owed was $14,153" (flagged as a balance-due claim) and named the prior year as "2024" in the observations, three attempts running. | Verifier: "tax owed" describing the liability line is not a direction claim. Prompt: the year rule now says "no other year; call the prior year last year", repeated in the observations block and the closing reminder. Both are cheap to keep and the golden scripts already follow them. | post-plan |
| Q43 | Kurt provided two real UltraTax client copies (2026-09-05, kept in the gitignored `tests/fixtures/real/`). Neither identified: no vendor string anywhere, the tax year read as 2026 from the cover letter's due dates, names came from the letter, and every profile line failed. | Findings applied to the profiles and the two readers, all verified on both returns and on the regenerated synthetic fixtures: (1) UltraTax client copies are 79 to 85 pages, the return itself starting a dozen pages in behind a summary, letters, filing instructions and the e-file authorization; a second copy of the federal pages follows the state return. Identify now locates the form page (IRS title plus OMB number) for year and names, and vendor signatures are matched across the whole package using UltraTax's report titles. (2) The real 2025 Form 1040 numbers its lines 7a, 11a, 11b, 12e, 13a, 13b (Schedule 1-A deductions), 27a; the base profile takes either numbering and the fixture generator prints the 2025 form. (3) UltraTax prints each amount as its own text object 4 points above the label and repeats the line number at x~478; the profile uses a tight row band, a value zone from x=500, and an anchored orphan-row rule. (4) Line 37 includes the line 38 penalty per the IRS instructions, and Missouri folds its underpayment penalty into the amount due; both reconciliation checks accept that identity and record it. (5) Missouri's MO-1040 runs five pages with the totals at the end and a cents column; state pages are now grouped with continuation pages and read across the group. (6) Two-year comparison reports span two pages, put values 4 points above labels, and sign the result ("Net tax due/-refund"). | post-plan |
| Q44 | The worker seeded `form-profiles/` onto the data volume once and never touched it again, so the live stack kept the v0.1.0 profiles after the image was rebuilt and the first real return failed with the old line numbers. An appliance update would do the same. | The entrypoint now reconciles the volume at every start: a profile identical to what the image last installed (kept under `.seeded/`) is replaced by the new version; an edited one is kept and named in the worker log with the newer version beside it. A volume from before `.seeded/` existed is refreshed wholesale (no firm has one yet). | post-plan |
| Q45 | Kurt (2026-09-05): "UltraTax only." | v1 accepts UltraTax CS client copies only. Staging skips a package whose software is recognised as anything else, or is unknown on a text-layer PDF, with a plain reason; a scanned package (no text layer) is let through with a warning and read with the UltraTax profile after OCR. The other five profiles stay in the repo, unvalidated, behind `ACCEPTED_SOFTWARE` in packages/shared. | post-plan |
| Q46 | Kurt (2026-09-05): thumbs up/down to monitor success; thumbs-down lists what went wrong; hold those for 90 days or until the admin dismisses; use failures to improve the process. | Per-user verdict per finished job (`job_feedback`), replaced on resend and audited. A thumbs-down needs at least one reason from a fixed list (numbers, direction, state, prior year, extraction, wording, tone, length, audio, slides, video, other + note) and holds every file of the job for 90 days past the retention windows, including "purge now"; an admin dismissal releases the hold and keeps the record. Settings › Quality shows the thumbs-up rate, reasons by count, open holds, and per case a diagnostic bundle (manifest with events and script attempts, extraction, verification, script, source PDF while on disk). The bundle is the input for profile and prompt fixes. Retention non-negotiable 5 stands: the hourly purge is still the only deleter; the hold just moves the date. | post-plan |
| Q47 | Kurt (2026-09-05): the default password on the Vibe Appliance card does not work. | Two causes, both ours. (1) The appliance seeds the first admin with `docker exec vibe-recap-api seed-admin`; `docker exec` bypasses the image entrypoint that dispatched that subcommand, so the exec failed ("executable file not found"), the enable logged a WARN, and no admin ever existed. The api image now ships `seed-admin`, `migrate` and `rotate-master-key` shims on PATH, the manifest's seed command goes through `docker-entrypoint.sh` so already-pulled images work too, and CI runs the shims against the built image. (2) The env template hard-coded `COOKIE_SECURE=true`; in LAN mode (plain HTTP) the browser accepts a Secure cookie and never returns it, so a sign-in would have looked right and then 401'd. The template now uses the appliance's `@SESSION_SECURE@` placeholder like Vibe-Connect and the router. Recovery on an installed box: `sudo docker exec vibe-recap-api docker-entrypoint.sh seed-admin`, and in LAN mode set `COOKIE_SECURE=false` in /opt/vibe/env/vibe-recap.env and restart the api (or re-enable the app after pulling the updated template). | post-plan |
| Q48 | Kurt (2026-09-05): "The app is missing a change password, password reset, and email outgoing (use emailit api)." PLAN §out-of-scope listed email sending and L12 limited the API's egress to the licensing server and the router. | Built as transactional email to firm users only, never clients. (1) Change password: `/account` page for every role (name in the header links to it); `POST /api/auth/change-password` existed and now also revokes reset tokens and sends a notice; a `mustChangePassword` flag from a temporary password forces the page before anything else. (2) Password reset: `POST /api/auth/forgot-password` (public, 5 per 15 min per IP, same answer for known and unknown addresses) emails a one-hour single-use link; the token is stored hashed in Redis, a new request revokes the previous one, reset ends every session, clears lockouts and the must-change flag, and is audited as `user.password_reset_self`. Admins get *Email reset link* next to *Set temporary password*. Invites are emailed too when email is on; the link is still shown. (3) Outgoing email through Emailit's v2 API (`POST https://api.emailit.com/v2/emails`, bearer key): Settings › Email holds provider, key (masked, write-only, excluded from export/import, redacted in logs; `EMAILIT_API_KEY` env as fallback), sender, reply-to, public URL, and a test send. L12 amended for the API container: `api.emailit.com` is its third permitted destination, contacted only once enabled. The worker is unchanged. | post-plan |
| Q49 | Kurt (2026-09-05): "Remove license key." L13 said the product is licensed via `licensing.kisaes.com`; Q31/Q35 had kept the check in place but unenforced. | Removed outright: the license routes and service, the daily check, the 402 read-only gate, the trial logic, the `license_key` setting, `LICENSE_SERVER_URL` / `LICENSE_ENFORCE`, the Settings › License page, and the `licenses` table (migration `0004_drop_licenses`). The product remains under the PolyForm Small Business License with no key and no phone-home; the API's outbound destinations are the Vibe AI Router and, when enabled, Emailit. L13 amended in PLAN.md; the appliance manifest no longer sets `LICENSE_ENFORCE` (the Vibe-Appliance copy needs the same edit). | post-plan |
| Q50 | Kurt (2026-09-11): the closing section read like an invitation to a meeting ("bring any questions to our meeting", "we will explain your payment options", "we will go through every number together at a comfortable pace"), and the voice rule told the model to defer questions to "your review". The recap is delivered *with* the finished return, not before a review appointment. | Re-framed the close: prompt rule 9 now says the final section states that a complete copy of the return accompanies the summary and that the office is glad to answer questions, and forbids mentioning a meeting, an appointment, a review session, payment options, or anything the client must do; the voice rule defers to "contact us with any questions". The default sign-off setting changed to "Please contact us with any questions." (firms that set their own keep it), the next slide's heading is "Your complete return copy is enclosed", and the three golden scripts were rewritten to match. PLAN.md §1 amended. | post-plan |
| Q51 | Kurt (2026-09-11): the narration voice was a single firm-wide setting (L19); a preparer may want a voice that matches them. | `users.voice` (nullable, migration `0005_user_voice`) holds a personal choice, set at *Your account* through `PUT /api/auth/preferences` and returned on `/api/auth/me`. The worker resolves the voice per job as uploader's choice -> firm setting -> `af_heart`, reading `users.voice` for `jobs.uploaded_by` at the `tts` step, so a re-render picks up a changed preference; an unbundled voice name falls back to the default. Settings › General still sets the firm default. L19 amended. | post-plan |
| Q52 | Kurt (2026-09-11): Kokoro read "$1,000" as "dollar one thousand" — espeak speaks the currency symbol where it stands. | `tts.speakable()` rewrites money into words just before synthesis ("1,000 dollars", "1 dollar", "1,234 dollars and 56 cents", "1.2 million dollars", "negative 500 dollars" for `-$500`/`($500)`). Only the audio is rewritten; the captions (`.vtt`) and transcript (`.txt`) keep the written `$1,000`, and the validator and verifier still see the unmodified script. Percentages already spoke correctly and are left alone. | post-plan |
