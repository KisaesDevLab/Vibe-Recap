# LAN mode: Caddy's internal CA issues a self-signed certificate.
# Browsers will warn once; import /data/caddy/pki/authorities/local/root.crt to trust it.
:80 {
	redir https://{host}{uri} permanent
}

:443 {
	tls internal {
		on_demand
	}
	import recap_routes
}
